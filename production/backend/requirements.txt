# FinTrust Hub 后端 Python 依赖 (与 pyproject.toml 同步)
# 安装: pip install -r requirements.txt
#
# 注意: 推荐使用 Python 3.11/3.12。Python 3.14 部分包尚无兼容版本。
# 带 [optional] 前缀的包为可选依赖，不影响核心启动；缺失时后端自动降级。

# Web 框架
fastapi[all]>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0

# 数据库 ORM + 迁移
sqlalchemy[asyncio]>=2.0.29
asyncpg>=0.29.0
alembic>=1.13.1
greenlet>=3.0.3

# 缓存 / 消息队列
redis[hiredis]>=5.0.3
kafka-python>=2.0.2
aio-pika>=9.4.0

# 数据校验
pydantic>=2.6.4
pydantic-settings>=2.2.1
email-validator>=2.1.1

# 时序数据库 / 图数据库
clickhouse-connect>=0.7.1
neo4j>=5.19.0

# AI / LLM
openai>=1.14.0
anthropic>=0.21.0
tiktoken>=0.6.0
# [optional] ML 依赖 — Python 3.14 可能无预编译 wheel，缺失时 scikit-learn/xgboost 功能降级
# scikit-learn>=1.4.1
# xgboost>=2.0.3
# pandas>=2.2.1

# 加密
cryptography>=42.0.5
pycryptodome>=3.20.0
pyjwt[crypto]>=2.8.0

# OCR / RPA
# [optional] PaddleOCR 需要 PaddlePaddle + 特定 CUDA/CPU 环境，缺失时降级到云 OCR
# paddleocr>=2.7.0
pdfplumber>=0.10.2
reportlab>=4.0.9
pypdf>=4.0.0

# 工作流编排
temporalio>=1.4.0
apscheduler>=3.10.4

# 区块链 SDK
# [optional] 蚂蚁链官方 SDK 通过 REST API 接入，无需 pip 包；接入时参考 docs/EXTERNAL_API_CONFIG.md
# antsdk>=1.0.0  # 已移除：PyPI 不存在此包名

# HTTP 客户端
httpx>=0.27.0
aiohttp>=3.9.3

# 工具
python-dotenv>=1.0.1
loguru>=0.7.2
rich>=13.7.1
orjson>=3.9.15
python-dateutil>=2.9.0
tenacity>=8.2.3
pyyaml>=6.0.1
